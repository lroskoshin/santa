# 🚀 После релиза WeSanta

Чек-лист задач, которые нужно выполнить после завершения разработки и перед публичным запуском.

---

## 🔓 Открыть сайт для поисковых роботов

### 1. Обновить `public/robots.txt`

**Было (закрыто):**
```
User-agent: *
Disallow: /
```

**Нужно (открыто):**
```
User-agent: *
Allow: /
Sitemap: https://wesanta.club/sitemap.xml
```

### 2. Обновить `app/layout.tsx`

Найти секцию `robots` и изменить:

```ts
// Robots — ОТКРЫТО для индексации
robots: {
  index: true,
  follow: true,
  googleBot: {
    index: true,
    follow: true,
    "max-video-preview": -1,
    "max-image-preview": "large",
    "max-snippet": -1,
  },
},
```

---

## 🗺️ Создать Sitemap

Создать файл `app/sitemap.ts`:

```ts
import { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: "https://wesanta.club",
      lastModified: new Date(),
      changeFrequency: "weekly",
      priority: 1,
    },
  ];
}
```

---

## 📊 Подключить аналитику

### Google Analytics / Yandex Metrika

1. Создать аккаунт в [Google Analytics](https://analytics.google.com/) или [Яндекс.Метрике](https://metrika.yandex.ru/)
2. Получить ID трекера
3. Добавить скрипт в `app/layout.tsx`

### Google Search Console

1. Зайти в [Google Search Console](https://search.google.com/search-console)
2. Добавить сайт `wesanta.club`
3. Подтвердить владение (через DNS или HTML-файл)
4. Отправить sitemap: `https://wesanta.club/sitemap.xml`

### Яндекс.Вебмастер

1. Зайти в [Яндекс.Вебмастер](https://webmaster.yandex.ru/)
2. Добавить сайт
3. Подтвердить владение
4. Отправить sitemap

---

## 🖼️ OG-изображение

Текущее решение — динамическая генерация через `next/og`. 

**Опционально:** можно заменить на статичную картинку для экономии ресурсов:
1. Сгенерировать PNG 1200×630
2. Положить в `public/og-image.png`
3. Обновить `layout.tsx` — добавить `images` в `openGraph` и `twitter`
4. Удалить `app/opengraph-image.tsx` и `app/twitter-image.tsx`

---

## 🔔 Дополнительно (по желанию)

- [ ] Настроить email-уведомления участникам
- [ ] Добавить страницу "О сервисе" / FAQ
- [ ] Добавить политику конфиденциальности
- [ ] Настроить мониторинг (Sentry, LogRocket)
- [ ] Добавить PWA манифест для установки на телефон
- [ ] Оптимизировать Core Web Vitals

---

## ✅ Проверить перед запуском

- [ ] Все страницы работают без ошибок
- [ ] Формы валидируются корректно
- [ ] Жеребьёвка работает правильно
- [ ] OG-превью отображается в соцсетях
- [ ] Сайт адаптивен на мобильных
- [ ] HTTPS работает корректно
- [ ] Домен `wesanta.club` настроен

---

*Файл создан: Декабрь 2024*



